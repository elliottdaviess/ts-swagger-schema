{
  "name": "ts-swagger-schema",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "generate-and-publish.js",
  "scripts": {
    "generate-ts": "npx swagger-typescript-api -p ./openapi.json -o ./ myApi.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "swagger-typescript-api": "^12.0.2"
  }
}